replicaCount: 1

image:
  repository: your-dockerhub-username/weather-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

env:
  OPENWEATHERMAP_API_KEY: your-api-key-here
  SERVER_PORT: "8080"

resources: {}

ingress:
  enabled: false
